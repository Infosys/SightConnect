import 'package:flutter/material.dart';

class TenantAdminRegisterTeamMemberPage extends StatelessWidget {
  const TenantAdminRegisterTeamMemberPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Register Team Member'),
      ),
      body: const SingleChildScrollView(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
