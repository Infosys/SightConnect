enum Status {
  ACTIVE,
  INACTIVE,
  ENTERED_IN_ERROR,
}

enum TestCategory {
  TRIAGE,
}

enum AvailabilityStatus {
  LOST,
  DAMAGED,
  DESTROYED,
  AVAILABLE,
}

enum UnitType{
  DIOPTER, LOGMAR, DECIMAL, SNELLEN
}

enum InputType{
  RANGE, TEXT, NUMBER, DISCRETE
}