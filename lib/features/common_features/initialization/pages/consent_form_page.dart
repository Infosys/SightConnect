import 'package:flutter/material.dart';

class ConsentFormPage extends StatelessWidget {
  const ConsentFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
