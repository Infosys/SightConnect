import 'package:flutter/material.dart';

class LandoltTestPage extends StatelessWidget {
  const LandoltTestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          "Landolt C Test",
        ),
      ),
    );
  }
}
