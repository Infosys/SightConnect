enum DynamicFormType {
  TEXTFIELD,
  RADIO,
  CHECKBOX,
  DROPDOWN,
  DATETIME,
  CHIPS,
  SLIDER,
  SWITCH,
  FILE,
  DEFAULT,
  TEXTAREA,
  CONDITIONAL,
  PANEL
}

enum FormLayoutType {
  STEPPER,
  PANEL,
}

enum FormPanelType {
  PANEL,
  REPEATED_PANEL,
}
