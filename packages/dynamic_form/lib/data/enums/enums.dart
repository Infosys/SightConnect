enum DynamicFormType {
  TEXTFIELD,
  RADIO,
  CHECKBOX,
  DROPDOWN,
  DATETIME,
  CHIPS,
  SLIDER,
  SWITCH,
  FILE,
  DEFAULT,
  TEXTAREA,
  CONDITIONAL,
  PANEL,
  DATE,
  DISPLAY
}

enum FormLayoutType {
  STEPPER,
  PANEL,
}

enum FormPanelType {
  PANEL,
  REPEATED_PANEL,
}

enum DynamicFormSavingType {
  DRAFT,
  SAVE,
}
